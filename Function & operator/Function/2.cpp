#include<iostream>
using namespace std;

int main()
{
    // Conditional operator 

    // int age ;
    // cin >> age;

    // (age >= 18) ? cout << "can vote " : cout << "can't vote " << endl;


    cout << "rhythm " << endl;
    continue;   // continue is used within a loop 
    cout<< "munjal "<<endl;
    
    return 0;

}